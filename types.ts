
export interface CountryData {
  id: string;
  name: string;
  code: string;
  flag: string;
  format: string;
  description: string;
  slug: string;
  example: string;
}

export interface GeneratedNumber {
  number: string;
  formatted: string;
}
